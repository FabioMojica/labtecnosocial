export const ALLOWED_ROLES = {
    superAdmin: 'super-admin',
    admin: 'admin',
    user: 'user',

    rolesArray: ["super-admin", "admin", "user"],

    onlyAdmins: ["super-admin", "admin"],
}

export const ALLOWED_STATES = {
    enabled: 'enabled', 
    disabled: 'disabled',

    statesArray: ["enabled", "disabled"],
} 